services:
  - type: web
    name: bethlehem-med-center-backend
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    plan: starter
    healthCheckPath: /health/simple
    envVars:
      - key: NODE_ENV
        value: production
